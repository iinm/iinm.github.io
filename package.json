{
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "type": "module",
  "scripts": {
    "check": "prettier --check --cache . && eslint . && tsc",
    "fix": "prettier --write --cache . && eslint --fix ."
  },
  "devDependencies": {
    "@eslint/js": "^9.4.0",
    "@types/node": "^20.14.2",
    "eslint": "^9.4.0",
    "globals": "^15.4.0",
    "prettier": "3.3.1",
    "typescript": "^5.5.1-rc"
  }
}
