{
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "npx @biomejs/biome check && tsc",
    "fix": "npx @biomejs/biome check --fix"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "@types/node": "^22.18.3",
    "globals": "^17.7.0",
    "typescript": "^7.0.2"
  }
}
