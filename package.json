{
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "type": "module",
  "scripts": {
    "check": "prettier --check --cache . && eslint . && tsc",
    "fix": "prettier --write --cache . && eslint --fix ."
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@types/node": "^22.9.3",
    "eslint": "^9.15.0",
    "globals": "^15.12.0",
    "prettier": "3.3.3",
    "typescript": "^5.7.2"
  }
}
