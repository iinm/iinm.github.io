{
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "type": "module",
  "scripts": {
    "check": "prettier --check --cache . && eslint . && tsc",
    "fix": "prettier --write --cache . && eslint --fix ."
  },
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "@types/node": "^22.18.3",
    "eslint": "^9.35.0",
    "globals": "^16.4.0",
    "prettier": "3.6.2",
    "typescript": "^5.9.2"
  }
}
